---
all:
  children:
    molecule:
      hosts:
        h1:
          mp:
            qemu:
              image: https://example.invalid/disk.qcow2
              driver: process
              network:
                mode: nat
  vars:
    mp_backend: qemu
    mp_defaults: {}
