---
all:
  children:
    molecule:
      hosts:
        h-broken:
          mp:
            docker:
              # image intentionally omitted
              privileged: true
  vars:
    mp_backend: docker
