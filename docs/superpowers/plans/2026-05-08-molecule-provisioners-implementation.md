# `david_igou.molecule_provisioners` v1.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn this collection into a reusable set of Molecule provisioners (podman + kubevirt). Consumers will install the collection, point their Molecule scenario's `create.yml`/`destroy.yml`/`prepare.yml` at one-line `import_playbook` wrappers, and pick the backend at runtime via `$PROVISIONER`.

**Architecture:** Three top-level dispatcher playbooks (`playbooks/{create,destroy,prepare}.yml`) read `$PROVISIONER`, validate platforms, and `include_role` into one of two roles (`roles/podman`, `roles/kubevirt`). Each role uses `tasks_from` for lifecycle dispatch (`create`/`destroy`/`prepare`). Self-tests live in `extensions/molecule/{podman,kubevirt}/` and are exercised by the existing `pytest tests/integration` machinery; the kubevirt scenario is gated behind `MOLECULE_KUBEVIRT_ENABLED`.

**Tech stack:** Ansible (>=2.15), `containers.podman`, `kubernetes.core`, `community.crypto`, Molecule with `driver: default, managed: true`, pytest-ansible, ansible-lint, antsibull-changelog.

**Reference implementation:** Logic for both backends is adapted from `david-igou/ansible-collection-devhost`'s `extensions/molecule/provisioners/{podman,kubevirt}/`. The plan reproduces the necessary YAML inline so the engineer doesn't need to fetch it.

**Working directory:** All paths are relative to the repo root `/workspace/ansible-collection-molecule_provisioners/`.

**Important environment note:** `ansible-test sanity` requires the collection to live at `<some_collections_path>/ansible_collections/david_igou/molecule_provisioners/`. The repo isn't naturally in that layout. Task 0 sets up a local symlink so tooling resolves correctly.

---

## File structure (after this plan)

```
.
├── CLAUDE.md                              (rewritten)
├── CHANGELOG.rst                          (regenerated by antsibull)
├── README.md                              (rewritten)
├── changelogs/fragments/v1.0.0.yml        (new)
├── docs/MIGRATION.md                      (new)
├── docs/examples/
│   ├── create.yml, destroy.yml, prepare.yml   (new — one-liners)
│   ├── molecule.yml                       (new)
│   └── platforms.yml                      (new)
├── docs/superpowers/...                   (specs/plans, untouched after this plan)
├── extensions/molecule/
│   ├── kubevirt/{molecule,converge,verify}.yml + create/destroy/prepare.yml
│   └── podman/{molecule,converge,verify}.yml + create/destroy/prepare.yml
├── galaxy.yml                             (updated: deps, metadata)
├── meta/runtime.yml                       (unchanged: requires_ansible >=2.15.0)
├── playbooks/
│   ├── create.yml, destroy.yml, prepare.yml   (new — dispatchers)
├── roles/
│   ├── kubevirt/
│   │   ├── README.md
│   │   ├── defaults/main.yml
│   │   ├── meta/{argument_specs.yml,main.yml}
│   │   └── tasks/{main.yml,create.yml,destroy.yml,prepare.yml,
│   │              _create_vm.yml,_create_vm_dictionary.yml}
│   └── podman/
│       ├── README.md
│       ├── defaults/main.yml
│       ├── meta/{argument_specs.yml,main.yml}
│       └── tasks/{main.yml,create.yml,destroy.yml,prepare.yml,_networks.yml}
├── tests/integration/
│   ├── conftest.py                        (new: gates kubevirt by env var)
│   └── test_integration.py                (unchanged)
├── tests/unit/test_basic.py               (unchanged)
└── (DELETED): plugins/, roles/run/, extensions/molecule/integration_hello_world/,
             extensions/molecule/utils/, tests/integration/targets/
```

---

## Task 0: Bootstrap workspace and baseline

**Files:**

- Create: `~/ansible_collections/david_igou/molecule_provisioners` (symlink to repo)

- [ ] **Step 1: Verify working directory and Ansible toolchain**

Run:

```bash
cd /workspace/ansible-collection-molecule_provisioners
ansible --version
ansible-lint --version
molecule --version
pytest --version
```

Expected: each command prints a version. Ansible >=2.15. If any are missing, install via `pip install -r requirements.txt -r test-requirements.txt ansible-core ansible-lint`. (In the hardened claude-run container these are pre-installed; only re-install if a command genuinely fails.)

- [ ] **Step 2: Create the canonical collection-path symlink**

Tooling (especially `ansible-test sanity` and FQCN resolution like `david_igou.molecule_provisioners.create`) requires the collection to live at `ansible_collections/david_igou/molecule_provisioners/` somewhere on `ANSIBLE_COLLECTIONS_PATH`.

Run:

```bash
mkdir -p "$HOME/ansible_collections/david_igou"
ln -snf /workspace/ansible-collection-molecule_provisioners \
        "$HOME/ansible_collections/david_igou/molecule_provisioners"
ls -l "$HOME/ansible_collections/david_igou/molecule_provisioners"
```

Expected: the symlink is listed and points at the repo.

Also append to your shell init (or set in this shell) so subsequent commands find it:

```bash
export ANSIBLE_COLLECTIONS_PATH="$HOME${ANSIBLE_COLLECTIONS_PATH:+:$ANSIBLE_COLLECTIONS_PATH}"
```

Verify:

```bash
ansible-galaxy collection list 2>&1 | grep david_igou
```

Expected output includes a line like `david_igou.molecule_provisioners 1.0.0`.

- [ ] **Step 3: Commit the ansible-creator scaffold as a baseline**

The repo currently contains untracked ansible-creator scaffolding. Snapshot it before deletion so history is meaningful.

Run:

```bash
git add -A
git status -s | head -40
```

Expected: ~30 files staged.

Commit:

```bash
git commit -m "$(cat <<'EOF'
Snapshot ansible-creator scaffold as v1.0 baseline

Captures the as-generated state from `ansible-creator` so subsequent
deletions and rewrites are visible in git history.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
git log --oneline | head -5
```

Expected: three commits total (spec, refinement, scaffold baseline).

---

## Task 1: Strip ansible-creator scaffolding

**Files:**

- Delete: `plugins/action/sample_action.py`, `plugins/filter/sample_filter.py`, `plugins/lookup/sample_lookup.py`, `plugins/test/sample_test.py`, `plugins/modules/sample_action.py`, `plugins/modules/sample_module.py`
- Delete: `plugins/cache/`, `plugins/inventory/`, `plugins/module_utils/`, `plugins/plugin_utils/`, `plugins/sub_plugins/` (all empty after sample removal)
- Delete: `roles/run/`
- Delete: `extensions/molecule/integration_hello_world/`
- Delete: `extensions/molecule/utils/`
- Delete: `tests/integration/targets/`

- [ ] **Step 1: Delete sample plugin files**

Run:

```bash
git rm plugins/action/sample_action.py \
       plugins/filter/sample_filter.py \
       plugins/lookup/sample_lookup.py \
       plugins/test/sample_test.py \
       plugins/modules/sample_action.py \
       plugins/modules/sample_module.py
```

Expected: each file shows as `rm 'plugins/...'`.

- [ ] **Step 2: Delete now-empty plugin subdirs**

The remaining `plugins/<dir>/__init__.py` files exist for empty dirs we don't need:

Run:

```bash
git rm -r plugins/cache/ plugins/inventory/ plugins/module_utils/ \
          plugins/plugin_utils/ plugins/sub_plugins/
```

Then check what's left:

```bash
ls plugins/
```

Expected: only `action/`, `filter/`, `lookup/`, `modules/`, `test/` remain (each with just `__init__.py`). We keep these because `ansible-test sanity` and `update-docs` expect a complete scaffold; they're empty placeholders.

- [ ] **Step 3: Delete the demo `run` role**

Run:

```bash
git rm -r roles/run/
ls roles/
```

Expected: `roles/` is empty (or doesn't exist after `rmdir` if git removed it).

If `roles/` directory itself remains:

```bash
rmdir roles/  # ok if it fails (we'll re-create roles/podman, roles/kubevirt later)
```

- [ ] **Step 4: Delete demo molecule + integration target**

Run:

```bash
git rm -r extensions/molecule/integration_hello_world/ \
          extensions/molecule/utils/ \
          tests/integration/targets/
ls extensions/molecule/ tests/integration/
```

Expected: `extensions/molecule/` is empty; `tests/integration/` contains only `__init__.py`, `test_integration.py`, `.gitignore`.

- [ ] **Step 5: Verify git status before commit**

Run:

```bash
git status -s
```

Expected: only deletions (lines starting with `D`). No `M` or `??` lines.

- [ ] **Step 6: Commit cleanup**

Run:

```bash
git commit -m "$(cat <<'EOF'
Remove ansible-creator demo scaffold

Drops sample plugins (sample_module, sample_filter, sample_lookup,
sample_test, sample_action), the demo `run` role, and the
integration_hello_world molecule scenario plus its target. The
collection's purpose is now provisioning automation only — these
demos would be noise.

Per the v1.0 design, `extensions/molecule/utils/` is also dropped
because the integration_-prefix indirection it provided no longer
fits the upcoming self-test scenarios (named after backends, not
prefixed).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

Expected: commit succeeds; `git log --oneline | head -1` shows this commit.

---

## Task 2: Update `galaxy.yml` and project metadata

**Files:**

- Modify: `galaxy.yml`
- Modify: `README.md` (full rewrite)
- Modify: `meta/runtime.yml` (no change — confirm)

- [ ] **Step 1: Rewrite `galaxy.yml`**

Replace the file contents with:

```yaml
---
namespace: david_igou
name: molecule_provisioners
version: 1.0.0
readme: README.md
authors:
  - David Igou <igou.david@gmail.com>

description: >-
  Reusable Molecule provisioner playbooks and roles (podman, kubevirt) for
  testing other Ansible collections without copy-pasting create/destroy/prepare
  automation per repo.

license_file: LICENSE
tags:
  - molecule
  - testing
  - podman
  - kubevirt

dependencies:
  containers.podman: ">=1.10.0"
  kubernetes.core: ">=3.0.0"
  community.crypto: ">=2.0.0"

repository: https://github.com/david-igou/ansible-collection-molecule_provisioners
documentation: https://github.com/david-igou/ansible-collection-molecule_provisioners
homepage: https://github.com/david-igou/ansible-collection-molecule_provisioners
issues: https://github.com/david-igou/ansible-collection-molecule_provisioners/issues

build_ignore:
  - .gitignore
  - changelogs/.plugin-cache.yaml
  - docs/superpowers
```

Note `docs/superpowers` is added to `build_ignore` so spec/plan files don't ship inside the Galaxy artifact.

- [ ] **Step 2: Verify `meta/runtime.yml` unchanged**

Run:

```bash
cat meta/runtime.yml
```

Expected:

```yaml
---
requires_ansible: ">=2.15.0"
```

No edit needed.

- [ ] **Step 3: Rewrite `README.md`**

Replace the file with:

````markdown
# david_igou.molecule_provisioners

Reusable [Molecule](https://ansible.readthedocs.io/projects/molecule/) provisioner playbooks and roles for testing Ansible collections.

Stop redefining `create.yml`/`destroy.yml`/`prepare.yml` per repo. Install this collection, write three one-line files in your scenario, and switch backends with one env var.

## Supported backends (v1.0)

| Backend            | When to use                                          |
| ------------------ | ---------------------------------------------------- |
| `podman` (default) | Containers, fastest CI loop                          |
| `kubevirt`         | Real VMs in a Kubernetes cluster (requires KubeVirt) |

## Installing

```bash
ansible-galaxy collection install david_igou.molecule_provisioners
```

Or via `requirements.yml`:

```yaml
collections:
  - name: david_igou.molecule_provisioners
    version: ">=1.0.0,<2.0.0"
```

## Using

In your collection's `extensions/molecule/<scenario>/` directory, create three one-line files:

```yaml
# create.yml
- import_playbook: david_igou.molecule_provisioners.create
```

```yaml
# destroy.yml
- import_playbook: david_igou.molecule_provisioners.destroy
```

```yaml
# prepare.yml
- import_playbook: david_igou.molecule_provisioners.prepare
```

Then point your scenario's `molecule.yml` at them and define your platforms with both `podman` and `kubevirt` blocks (consumers can omit a block if they never use that backend):

```yaml
provisioner:
  name: ansible
  playbooks:
    create: create.yml
    destroy: destroy.yml
    prepare: prepare.yml
    converge: converge.yml

platforms:
  - name: ubuntu-24
    podman:
      image: docker.io/geerlingguy/docker-ubuntu2404-ansible:latest
      command: sleep 1d
    kubevirt:
      image: quay.io/containerdisks/ubuntu:24.04
      namespace: molecule
      ssh_service:
        type: NodePort
      ansible_user: cloud-user
      memory: 4Gi
      disk_size: 30Gi
```

Switch backends per run:

```bash
PROVISIONER=podman   molecule test    # default
PROVISIONER=kubevirt molecule test
```

A complete starter template is in [`docs/examples/`](docs/examples/). To migrate an existing collection, see [`docs/MIGRATION.md`](docs/MIGRATION.md).

## What's in the box

- `playbooks/{create,destroy,prepare}.yml` — top-level dispatchers; read `$PROVISIONER`, validate, dispatch.
- `roles/podman/` — uses `containers.podman.podman_container` + `podman_network`.
- `roles/kubevirt/` — generates an SSH keypair, creates `VirtualMachine` + `NodePort` Service per platform, writes the molecule inventory file.

Both roles produce a host group named `molecule` containing all platform hosts.

## Out of scope for v1.0

- docker, qemu/libvirt, AWS, Azure, GCP backends
- LoadBalancer / ClusterIP+port-forward kubevirt service types
- Windows/macOS guests
- Per-platform networks beyond `podman.podman_network`
- Molecule `shared_state` / shared default-scenario pattern

## Licensing

GPL v3.0 or later — see [LICENSE](LICENSE).
````

- [ ] **Step 4: Quick-validate the YAML files**

Run:

```bash
yamllint galaxy.yml meta/runtime.yml
```

Expected: no errors. (Warnings on doc length are tolerable.)

- [ ] **Step 5: Commit**

Run:

```bash
git add galaxy.yml README.md
git commit -m "$(cat <<'EOF'
Rewrite galaxy.yml and README for v1.0

galaxy.yml:
- Real description, tags, repo/issues URLs.
- Dependencies: containers.podman, kubernetes.core, community.crypto.
  Drop ansible.utils (unused).
- build_ignore docs/superpowers so spec/plan files don't ship.

README.md: replaces ansible-creator boilerplate with concrete
usage — the three one-liner files, the multi-keyed platform schema,
and the $PROVISIONER toggle.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: Create the `podman` role — defaults, argument_specs, README

**Files:**

- Create: `roles/podman/defaults/main.yml`
- Create: `roles/podman/meta/main.yml`
- Create: `roles/podman/meta/argument_specs.yml`
- Create: `roles/podman/README.md`

- [ ] **Step 1: `roles/podman/defaults/main.yml`**

```yaml
---
# Defaults for david_igou.molecule_provisioners.podman.
# These are the role-level overrides; per-platform values come from
# molecule.yml platforms[].podman.*

# Reserved network names that must NOT be (re)created/deleted as standalone networks.
mp_podman_reserved_networks:
  - bridge
  - none
  - host
  - slirp4netns

# Async timing for create/destroy bulk operations (seconds + retry count).
mp_podman_async_timeout: 7200
mp_podman_async_retries: 300
```

- [ ] **Step 2: `roles/podman/meta/main.yml`**

```yaml
---
galaxy_info:
  role_name: podman
  author: David Igou
  description: Molecule provisioner role using containers.podman
  company: ""
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: GenericLinux
      versions: [all]
  galaxy_tags:
    - molecule
    - podman
    - testing
dependencies: []
```

- [ ] **Step 3: `roles/podman/meta/argument_specs.yml`**

Argument spec validates the role _invocation_ parameters. We keep this minimal (the role takes `molecule_yml` from outer scope and reads `platforms[].podman.*` itself; argument_specs covers the few overridable role vars).

```yaml
---
argument_specs:
  main:
    short_description: >-
      Default entry point — does nothing on its own; use tasks_from=create|destroy|prepare.
    options: {}
  create:
    short_description: Create podman containers and networks for molecule platforms.
    options:
      mp_podman_async_timeout:
        type: int
        default: 7200
        description: Async timeout (seconds) for bulk container create.
      mp_podman_async_retries:
        type: int
        default: 300
        description: Number of times to poll for async completion.
  destroy:
    short_description: Destroy podman containers and (non-reserved) networks.
    options:
      mp_podman_async_timeout:
        type: int
        default: 7200
      mp_podman_async_retries:
        type: int
        default: 300
  prepare:
    short_description: Run podman-side preparation tasks against created containers.
    options: {}
```

- [ ] **Step 4: `roles/podman/README.md`**

````markdown
# `david_igou.molecule_provisioners.podman`

Molecule provisioner role for podman containers. Not invoked directly — invoked via the collection's top-level `playbooks/{create,destroy,prepare}.yml` dispatchers (which set `PROVISIONER=podman`).

## Entry points

| `tasks_from` | What it does                                                                                                      |
| ------------ | ----------------------------------------------------------------------------------------------------------------- |
| `create`     | Creates user-defined podman networks (when set), then creates containers from `molecule_yml.platforms[].podman.*` |
| `destroy`    | Removes those containers and any non-reserved networks                                                            |
| `prepare`    | Installs `sudo` inside each container                                                                             |

## Inputs (per-platform, in `molecule.yml`)

```yaml
platforms:
  - name: ubuntu-24
    podman:
      image: docker.io/...:tag # required
      command: sleep 1d # optional
      privileged: false # optional, default false
      volumes: [] # optional
      capabilities: [] # optional
      podman_network: [] # optional, list or single string
      env: {} # optional
      tmpfs: [] # optional
      exposed_ports: [] # optional
      published_ports: [] # optional
```

## Role-level overrides

See `defaults/main.yml`.
````

- [ ] **Step 5: Quick syntax check**

Run:

```bash
ansible-lint roles/podman/
```

Expected: passes (or warnings only). The `tasks/` files don't exist yet — ansible-lint should report no errors but may note missing tasks. If it errors on missing tasks, ignore for now; we add them in Task 4.

- [ ] **Step 6: Commit**

```bash
git add roles/podman/
git commit -m "$(cat <<'EOF'
Add podman role scaffold (defaults, meta, argument_specs, README)

No tasks yet — those land in the next commit. This split keeps the
diff small enough to review the public contract (defaults +
argument_specs + README) independent of implementation.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: Implement `roles/podman/tasks/`

**Files:**

- Create: `roles/podman/tasks/main.yml`
- Create: `roles/podman/tasks/create.yml`
- Create: `roles/podman/tasks/destroy.yml`
- Create: `roles/podman/tasks/prepare.yml`
- Create: `roles/podman/tasks/_networks.yml`

Logic mirrors `david-igou/ansible-collection-devhost/extensions/molecule/provisioners/podman/{create,destroy,prepare}.yml` (verified before plan-writing) with shared network logic factored into `_networks.yml`.

- [ ] **Step 1: `roles/podman/tasks/main.yml`**

The default entry point exists so `ansible-lint` doesn't complain. It does nothing — callers must specify `tasks_from`.

```yaml
---
- name: "podman role: no default entry point"
  ansible.builtin.fail:
    msg: >-
      david_igou.molecule_provisioners.podman has no main entry point.
      Use tasks_from=create|destroy|prepare via include_role.
```

- [ ] **Step 2: `roles/podman/tasks/_networks.yml`**

Shared helper that builds `__mp_podman_networks` from `molecule_yml.platforms[].podman.podman_network`. Used by both create and destroy.

```yaml
---
- name: Initialize podman network list
  ansible.builtin.set_fact:
    __mp_podman_networks: []

- name: Collect networks from platforms[].podman.podman_network
  ansible.builtin.set_fact:
    __mp_podman_networks: >-
      {{ __mp_podman_networks +
         (item.podman.podman_network
          if (item.podman.podman_network is sequence and item.podman.podman_network is not string)
          else [item.podman.podman_network]) }}
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    label: "{{ item.name }}"
  when: item.podman.podman_network is defined
```

- [ ] **Step 3: `roles/podman/tasks/create.yml`**

```yaml
---
- name: Build podman network list
  ansible.builtin.import_tasks: _networks.yml

- name: Create podman network(s)
  containers.podman.podman_network:
    name: "{{ item }}"
    state: present
  loop: "{{ __mp_podman_networks | default([]) | unique }}"
  when:
    - __mp_podman_networks | default([]) | length > 0
    - item not in mp_podman_reserved_networks
    - item is not match('^ns:')
    - item is not match('^container:')

- name: Create molecule instance(s)
  containers.podman.podman_container:
    name: "{{ item.name }}"
    image: "{{ item.podman.image }}"
    state: started
    recreate: false
    command: "{{ item.podman.command | default(omit) }}"
    privileged: "{{ item.podman.privileged | default(false) }}"
    volume: "{{ item.podman.volumes | default(omit) }}"
    cap_add: "{{ item.podman.capabilities | default(omit) }}"
    expose: "{{ item.podman.exposed_ports | default(omit) }}"
    publish: "{{ item.podman.published_ports | default(omit) }}"
    network: "{{ item.podman.podman_network | default(omit) }}"
    env: "{{ item.podman.env | default({}) }}"
    tmpfs: "{{ item.podman.tmpfs | default(omit) }}"
  register: __mp_podman_create
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    label: "{{ item.name }}"
  async: "{{ mp_podman_async_timeout }}"
  poll: 0

- name: Wait for instance(s) creation to complete
  ansible.builtin.async_status:
    jid: "{{ item.ansible_job_id }}"
  register: __mp_podman_jobs
  until: __mp_podman_jobs.finished
  retries: "{{ mp_podman_async_retries }}"
  loop: "{{ __mp_podman_create.results }}"
  loop_control:
    label: "{{ item.item.name }}"
```

- [ ] **Step 4: `roles/podman/tasks/destroy.yml`**

```yaml
---
- name: Destroy molecule instance(s)
  containers.podman.podman_container:
    name: "{{ item.name }}"
    state: absent
  register: __mp_podman_destroy
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    label: "{{ item.name }}"
  async: "{{ mp_podman_async_timeout }}"
  poll: 0

- name: Wait for instance(s) deletion to complete
  ansible.builtin.async_status:
    jid: "{{ item.ansible_job_id }}"
  register: __mp_podman_jobs
  until: __mp_podman_jobs.finished
  retries: "{{ mp_podman_async_retries }}"
  loop: "{{ __mp_podman_destroy.results }}"
  loop_control:
    label: "{{ item.item.name }}"

- name: Build podman network list
  ansible.builtin.import_tasks: _networks.yml

- name: Delete podman network(s)
  containers.podman.podman_network:
    name: "{{ item }}"
    state: absent
  loop: "{{ __mp_podman_networks | default([]) | unique }}"
  when:
    - __mp_podman_networks | default([]) | length > 0
    - item not in mp_podman_reserved_networks
    - item is not match('^ns:')
    - item is not match('^container:')
```

- [ ] **Step 5: `roles/podman/tasks/prepare.yml`**

```yaml
---
- name: Ensure sudo is installed
  ansible.builtin.package:
    name: sudo
    state: present
```

- [ ] **Step 6: Lint**

Run:

```bash
ansible-lint roles/podman/
```

Expected: no errors. Warnings about idempotency on `package` are acceptable (the `state: present` is the canonical idiom).

If errors: read them and fix before proceeding.

- [ ] **Step 7: Syntax check the role**

Create a throwaway test playbook:

```bash
cat > /tmp/_syntax_check.yml <<'EOF'
- hosts: localhost
  gather_facts: false
  tasks:
    - ansible.builtin.include_role:
        name: david_igou.molecule_provisioners.podman
        tasks_from: create
      vars:
        molecule_yml:
          platforms: []
EOF
ansible-playbook --syntax-check /tmp/_syntax_check.yml
rm /tmp/_syntax_check.yml
```

Expected: `playbook: /tmp/_syntax_check.yml` printed; no syntax errors.

- [ ] **Step 8: Commit**

```bash
git add roles/podman/tasks/
git commit -m "$(cat <<'EOF'
Implement podman role tasks (create/destroy/prepare/_networks)

Lifts the network/container management from the devhost reference
implementation into a reusable role. Networks are factored into
_networks.yml so create.yml and destroy.yml share the platform-list
walk. Reserved network names live in defaults so users can extend
the list per project.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: Create the `kubevirt` role — defaults, argument_specs, README

**Files:**

- Create: `roles/kubevirt/defaults/main.yml`
- Create: `roles/kubevirt/meta/main.yml`
- Create: `roles/kubevirt/meta/argument_specs.yml`
- Create: `roles/kubevirt/README.md`

- [ ] **Step 1: `roles/kubevirt/defaults/main.yml`**

```yaml
---
# Defaults for david_igou.molecule_provisioners.kubevirt.

# SSH keypair lives in the molecule ephemeral dir so destroy can find it.
mp_kubevirt_ssh_key_path: "{{ molecule_ephemeral_directory }}/identity_file"

# Default namespace if a platform doesn't set its own.
mp_kubevirt_namespace: molecule

# wait_for_connection timeout (seconds) for prepare phase.
mp_kubevirt_wait_timeout: 120

# Allowed ssh_service.type values. v1 supports NodePort only.
mp_kubevirt_allowed_ssh_service_types:
  - NodePort
```

- [ ] **Step 2: `roles/kubevirt/meta/main.yml`**

```yaml
---
galaxy_info:
  role_name: kubevirt
  author: David Igou
  description: Molecule provisioner role using KubeVirt VMs in Kubernetes
  company: ""
  license: GPL-3.0-or-later
  min_ansible_version: "2.15"
  platforms:
    - name: GenericLinux
      versions: [all]
  galaxy_tags:
    - molecule
    - kubevirt
    - kubernetes
    - testing
dependencies: []
```

- [ ] **Step 3: `roles/kubevirt/meta/argument_specs.yml`**

```yaml
---
argument_specs:
  main:
    short_description: >-
      Default entry point — does nothing on its own; use tasks_from=create|destroy|prepare.
    options: {}
  create:
    short_description: Create KubeVirt VirtualMachines and NodePort services for molecule platforms.
    options:
      mp_kubevirt_ssh_key_path:
        type: path
        description: Where the SSH keypair is generated (defaults to molecule_ephemeral_directory/identity_file).
      mp_kubevirt_namespace:
        type: str
        default: molecule
        description: Default namespace if a platform's kubevirt block doesn't set one.
  destroy:
    short_description: Delete KubeVirt VirtualMachines and NodePort services.
    options:
      mp_kubevirt_namespace:
        type: str
        default: molecule
  prepare:
    short_description: Wait for KubeVirt-provisioned hosts to be reachable.
    options:
      mp_kubevirt_wait_timeout:
        type: int
        default: 120
```

- [ ] **Step 4: `roles/kubevirt/README.md`**

````markdown
# `david_igou.molecule_provisioners.kubevirt`

Molecule provisioner role for KubeVirt VMs. Not invoked directly — invoked via the collection's top-level `playbooks/{create,destroy,prepare}.yml` dispatchers (which set `PROVISIONER=kubevirt`).

Requires:

- A reachable Kubernetes cluster with KubeVirt installed and a working `KUBECONFIG`.
- `kubernetes.core` and `community.crypto` collections (declared as deps in `galaxy.yml`).

## Entry points

| `tasks_from` | What it does                                                                                             |
| ------------ | -------------------------------------------------------------------------------------------------------- |
| `create`     | Generates SSH keypair, creates VirtualMachine + NodePort Service per platform, writes molecule inventory |
| `destroy`    | Deletes VirtualMachine and NodePort Service per platform                                                 |
| `prepare`    | `wait_for_connection` against each created host                                                          |

## Inputs (per-platform, in `molecule.yml`)

```yaml
platforms:
  - name: ubuntu-24
    kubevirt:
      image: quay.io/containerdisks/ubuntu:24.04 # required, containerdisk image
      namespace: molecule # required
      ansible_user: cloud-user # required
      memory: 4Gi # required
      disk_size: 30Gi # required
      ssh_service:
        type: NodePort # only NodePort supported in v1
```

## Role-level overrides

See `defaults/main.yml`.

## v1 limitation

Only `ssh_service.type: NodePort` is supported. The role asserts this on create. LoadBalancer / ClusterIP+port-forward are out of scope for v1.
````

- [ ] **Step 5: Lint and commit**

```bash
ansible-lint roles/kubevirt/
git add roles/kubevirt/
git commit -m "$(cat <<'EOF'
Add kubevirt role scaffold (defaults, meta, argument_specs, README)

Public contract first; tasks land in the next commit.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: Implement `roles/kubevirt/tasks/`

**Files:**

- Create: `roles/kubevirt/tasks/main.yml`
- Create: `roles/kubevirt/tasks/create.yml`
- Create: `roles/kubevirt/tasks/destroy.yml`
- Create: `roles/kubevirt/tasks/prepare.yml`
- Create: `roles/kubevirt/tasks/_create_vm.yml`
- Create: `roles/kubevirt/tasks/_create_vm_dictionary.yml`

Logic mirrors `david-igou/ansible-collection-devhost/extensions/molecule/provisioners/kubevirt/` plus the inline service-type assert from the spec.

- [ ] **Step 1: `roles/kubevirt/tasks/main.yml`**

```yaml
---
- name: "kubevirt role: no default entry point"
  ansible.builtin.fail:
    msg: >-
      david_igou.molecule_provisioners.kubevirt has no main entry point.
      Use tasks_from=create|destroy|prepare via include_role.
```

- [ ] **Step 2: `roles/kubevirt/tasks/create.yml`**

```yaml
---
- name: Validate ssh_service.type per platform
  ansible.builtin.assert:
    that:
      - vm.kubevirt.ssh_service.type | default('NodePort')
        in mp_kubevirt_allowed_ssh_service_types
    fail_msg: >-
      Platform '{{ vm.name }}' has unsupported ssh_service.type
      '{{ vm.kubevirt.ssh_service.type }}'. v1 supports only:
      {{ mp_kubevirt_allowed_ssh_service_types | join(', ') }}.
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"

- name: Generate SSH key pair
  community.crypto.openssh_keypair:
    path: "{{ mp_kubevirt_ssh_key_path }}"
    size: 2048
  register: __mp_kubevirt_ssh_keypair

- name: Set SSH public key fact
  ansible.builtin.set_fact:
    temporary_ssh_public_key: "{{ __mp_kubevirt_ssh_keypair.public_key }}"

- name: Create VirtualMachine in KubeVirt
  ansible.builtin.include_tasks: _create_vm.yml
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"

- name: Create SSH NodePort Kubernetes service
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Service
      metadata:
        name: "{{ vm.name }}"
        namespace: "{{ vm.kubevirt.namespace | default(mp_kubevirt_namespace) }}"
      spec:
        ports:
          - port: 22
            protocol: TCP
            targetPort: 22
        selector:
          kubevirt.io/domain: "{{ vm.name }}"
        type: NodePort
  when: vm.kubevirt.ssh_service.type | default('') == 'NodePort'
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"

- name: Build VM dictionary entries
  ansible.builtin.include_tasks: _create_vm_dictionary.yml
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"

- name: Create Ansible inventory from dictionary
  vars:
    molecule_inventory:
      all:
        children:
          molecule:
            hosts: "{{ __mp_kubevirt_molecule_systems | default({}) }}"
  ansible.builtin.copy:
    content: "{{ molecule_inventory | to_nice_yaml }}"
    dest: "{{ molecule_ephemeral_directory }}/inventory/molecule_inventory.yml"
    mode: "0600"

- name: Refresh inventory
  ansible.builtin.meta: refresh_inventory

- name: Assert molecule group exists in inventory
  ansible.builtin.assert:
    that: "'molecule' in groups"
    fail_msg: "Molecule group was not found in inventory groups: {{ groups }}"
  run_once: true # noqa: run-once[task]
```

- [ ] **Step 3: `roles/kubevirt/tasks/_create_vm.yml`**

This file is the per-VM body. It uses `vm` as the loop var (set in the include_tasks above). The VM definition follows KubeVirt's standard schema with cloud-init for SSH.

```yaml
---
- name: "Define VirtualMachine: {{ vm.name }}"
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: kubevirt.io/v1
      kind: VirtualMachine
      metadata:
        name: "{{ vm.name }}"
        namespace: "{{ vm.kubevirt.namespace | default(mp_kubevirt_namespace) }}"
        labels:
          kubevirt.io/domain: "{{ vm.name }}"
      spec:
        running: true
        template:
          metadata:
            labels:
              kubevirt.io/domain: "{{ vm.name }}"
          spec:
            domain:
              cpu:
                cores: 2
              resources:
                requests:
                  memory: "{{ vm.kubevirt.memory }}"
              devices:
                disks:
                  - name: containerdisk
                    disk: { bus: virtio }
                  - name: cloudinitdisk
                    disk: { bus: virtio }
                interfaces:
                  - name: default
                    masquerade: {}
            networks:
              - name: default
                pod: {}
            volumes:
              - name: containerdisk
                containerDisk:
                  image: "{{ vm.kubevirt.image }}"
              - name: cloudinitdisk
                cloudInitNoCloud:
                  userData: |
                    #cloud-config
                    user: {{ vm.kubevirt.ansible_user }}
                    ssh_authorized_keys:
                      - {{ temporary_ssh_public_key }}
                    chpasswd:
                      expire: false
```

- [ ] **Step 4: `roles/kubevirt/tasks/_create_vm_dictionary.yml`**

Builds an inventory entry for the current `vm`. Uses the NodePort retrieved earlier and the cluster's external IP (read from the service's first node IP via `nodePort` + `status.loadBalancer` or via the cluster default). For NodePort we look up the node IP from the K8s service info.

```yaml
---
- name: Initialize systems dict on first iteration
  ansible.builtin.set_fact:
    __mp_kubevirt_molecule_systems: "{{ __mp_kubevirt_molecule_systems | default({}) }}"

- name: "Look up service for {{ vm.name }} (for NodePort + nodeIP)"
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Service
    name: "{{ vm.name }}"
    namespace: "{{ vm.kubevirt.namespace | default(mp_kubevirt_namespace) }}"
  register: __mp_kubevirt_svc

- name: "Look up a Ready node (for NodePort connection)"
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Node
  register: __mp_kubevirt_nodes
  run_once: true

- name: "Choose first node InternalIP"
  ansible.builtin.set_fact:
    __mp_kubevirt_node_ip: >-
      {{ (__mp_kubevirt_nodes.resources[0].status.addresses
          | selectattr('type','equalto','InternalIP') | list | first).address }}
  run_once: true

- name: "Add {{ vm.name }} to molecule inventory dict"
  ansible.builtin.set_fact:
    __mp_kubevirt_molecule_systems: >-
      {{ __mp_kubevirt_molecule_systems | combine({
          vm.name: {
            'ansible_host': __mp_kubevirt_node_ip,
            'ansible_port': (__mp_kubevirt_svc.resources[0].spec.ports[0].nodePort),
            'ansible_user': vm.kubevirt.ansible_user,
            'ansible_ssh_private_key_file': mp_kubevirt_ssh_key_path,
            'ansible_ssh_common_args': '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
          }
      }) }}
```

- [ ] **Step 5: `roles/kubevirt/tasks/destroy.yml`**

```yaml
---
- name: Delete VirtualMachine in KubeVirt
  kubernetes.core.k8s:
    state: absent
    api_version: kubevirt.io/v1
    kind: VirtualMachine
    name: "{{ vm.name }}"
    namespace: "{{ vm.kubevirt.namespace | default(mp_kubevirt_namespace) }}"
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"

- name: Delete NodePort service in KubeVirt
  kubernetes.core.k8s:
    state: absent
    api_version: v1
    kind: Service
    name: "{{ vm.name }}"
    namespace: "{{ vm.kubevirt.namespace | default(mp_kubevirt_namespace) }}"
  loop: "{{ molecule_yml.platforms }}"
  loop_control:
    loop_var: vm
    label: "{{ vm.name }}"
```

- [ ] **Step 6: `roles/kubevirt/tasks/prepare.yml`**

```yaml
---
- name: Wait for the host to be reachable
  ansible.builtin.wait_for_connection:
    timeout: "{{ mp_kubevirt_wait_timeout }}"
```

- [ ] **Step 7: Lint and syntax check**

```bash
ansible-lint roles/kubevirt/
```

Then a syntax check using a stub playbook (no cluster needed for syntax):

```bash
cat > /tmp/_kv_syntax.yml <<'EOF'
- hosts: localhost
  gather_facts: false
  vars:
    molecule_ephemeral_directory: /tmp
  tasks:
    - ansible.builtin.include_role:
        name: david_igou.molecule_provisioners.kubevirt
        tasks_from: destroy
      vars:
        molecule_yml:
          platforms: []
EOF
ansible-playbook --syntax-check /tmp/_kv_syntax.yml
rm /tmp/_kv_syntax.yml
```

Expected: no syntax errors. `ansible-lint` may warn about `run_once` in a loop context — those are noqa-tagged or accepted.

- [ ] **Step 8: Commit**

```bash
git add roles/kubevirt/tasks/
git commit -m "$(cat <<'EOF'
Implement kubevirt role tasks (create/destroy/prepare + helpers)

create.yml: validate ssh_service.type, generate SSH keypair, create
VirtualMachines and NodePort Services per platform, build the
molecule inventory file, and refresh inventory.

destroy.yml: tear down the VirtualMachine and Service objects.

prepare.yml: wait_for_connection.

Helper task files:
- _create_vm.yml: KubeVirt VirtualMachine definition with cloud-init.
- _create_vm_dictionary.yml: builds inventory entries using the
  cluster node InternalIP + service NodePort.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: Top-level dispatcher playbooks

**Files:**

- Create: `playbooks/create.yml`
- Create: `playbooks/destroy.yml`
- Create: `playbooks/prepare.yml`

- [ ] **Step 1: `playbooks/create.yml`**

```yaml
---
- name: Molecule provisioner — create
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    _mp_provisioner: "{{ lookup('env', 'PROVISIONER') | default(mp_default_provisioner | default('podman'), true) }}"
    _mp_supported: [podman, kubevirt]
  tasks:
    - name: Validate PROVISIONER
      ansible.builtin.assert:
        that: _mp_provisioner in _mp_supported
        fail_msg: >-
          PROVISIONER must be one of {{ _mp_supported | join(', ') }}
          (got '{{ _mp_provisioner }}').

    - name: Validate every platform has the active provisioner's key
      ansible.builtin.assert:
        that: item[_mp_provisioner] is defined
        fail_msg: >-
          Platform '{{ item.name }}' is missing a '{{ _mp_provisioner }}'
          block in molecule.yml.
      loop: "{{ molecule_yml.platforms }}"
      loop_control:
        label: "{{ item.name }}"

    - name: Run provisioner create
      ansible.builtin.include_role:
        name: "david_igou.molecule_provisioners.{{ _mp_provisioner }}"
        tasks_from: create
```

- [ ] **Step 2: `playbooks/destroy.yml`**

```yaml
---
- name: Molecule provisioner — destroy
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    _mp_provisioner: "{{ lookup('env', 'PROVISIONER') | default(mp_default_provisioner | default('podman'), true) }}"
    _mp_supported: [podman, kubevirt]
  tasks:
    - name: Validate PROVISIONER
      ansible.builtin.assert:
        that: _mp_provisioner in _mp_supported
        fail_msg: >-
          PROVISIONER must be one of {{ _mp_supported | join(', ') }}
          (got '{{ _mp_provisioner }}').

    - name: Run provisioner destroy
      ansible.builtin.include_role:
        name: "david_igou.molecule_provisioners.{{ _mp_provisioner }}"
        tasks_from: destroy
```

(Destroy intentionally skips the per-platform key assert: destroy tolerates platforms missing their backend block, since `state: absent` is a no-op for things that were never created.)

- [ ] **Step 3: `playbooks/prepare.yml`**

```yaml
---
- name: Molecule provisioner — prepare
  hosts: all
  gather_facts: false
  vars:
    _mp_provisioner: "{{ lookup('env', 'PROVISIONER') | default(mp_default_provisioner | default('podman'), true) }}"
    _mp_supported: [podman, kubevirt]
  tasks:
    - name: Validate PROVISIONER
      ansible.builtin.assert:
        that: _mp_provisioner in _mp_supported
        fail_msg: >-
          PROVISIONER must be one of {{ _mp_supported | join(', ') }}
          (got '{{ _mp_provisioner }}').

    - name: Run provisioner prepare
      ansible.builtin.include_role:
        name: "david_igou.molecule_provisioners.{{ _mp_provisioner }}"
        tasks_from: prepare
```

- [ ] **Step 4: Lint**

```bash
ansible-lint playbooks/
```

Expected: clean.

- [ ] **Step 5: Sanity-check FQCN resolution**

Confirm the playbooks resolve as a collection-shipped namespace:

```bash
ansible-playbook --syntax-check \
  "$HOME/ansible_collections/david_igou/molecule_provisioners/playbooks/create.yml"
```

Expected: no errors. (The play will fail at run time without `molecule_yml`; we're only checking parse-time.)

- [ ] **Step 6: Commit**

```bash
git add playbooks/
git commit -m "$(cat <<'EOF'
Add top-level dispatcher playbooks (create/destroy/prepare)

These are the entry points consumers reference via:
  - import_playbook: david_igou.molecule_provisioners.create

Each reads $PROVISIONER (default podman), validates it, and dispatches
to the matching role's tasks_from=<phase>. create.yml additionally
asserts every platform has the active provisioner's key.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: Self-test scenario — `extensions/molecule/podman/`

This scenario exercises the full create→prepare→converge→verify→destroy lifecycle against podman. It runs in CI.

**Files:**

- Create: `extensions/molecule/podman/molecule.yml`
- Create: `extensions/molecule/podman/create.yml`
- Create: `extensions/molecule/podman/destroy.yml`
- Create: `extensions/molecule/podman/prepare.yml`
- Create: `extensions/molecule/podman/converge.yml`
- Create: `extensions/molecule/podman/verify.yml`

- [ ] **Step 1: `extensions/molecule/podman/molecule.yml`**

```yaml
---
driver:
  name: default
  options:
    managed: true
    ansible_connection_options:
      connection: local

platforms:
  - name: ubuntu-24
    podman:
      image: docker.io/geerlingguy/docker-ubuntu2404-ansible:latest
      command: sleep 1d
  - name: centos-stream-10
    podman:
      image: quay.io/centos/centos:stream10
      command: sleep 1d

provisioner:
  name: ansible
  playbooks:
    create: create.yml
    destroy: destroy.yml
    prepare: prepare.yml
    converge: converge.yml
    verify: verify.yml
  env:
    PROVISIONER: podman

verifier:
  name: ansible

scenario:
  name: podman
  test_sequence:
    - dependency
    - syntax
    - create
    - prepare
    - converge
    - verify
    - destroy
```

- [ ] **Step 2: `extensions/molecule/podman/create.yml`**

```yaml
---
- import_playbook: david_igou.molecule_provisioners.create
```

- [ ] **Step 3: `extensions/molecule/podman/destroy.yml`**

```yaml
---
- import_playbook: david_igou.molecule_provisioners.destroy
```

- [ ] **Step 4: `extensions/molecule/podman/prepare.yml`**

```yaml
---
- import_playbook: david_igou.molecule_provisioners.prepare
```

- [ ] **Step 5: `extensions/molecule/podman/converge.yml`**

The converge in a self-test asserts the molecule group has the right hosts and they're reachable. It does not run any user role.

```yaml
---
- name: Verify molecule group composition
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Assert molecule group has both expected hosts
      ansible.builtin.assert:
        that:
          - "'molecule' in groups"
          - "'ubuntu-24' in groups['molecule']"
          - "'centos-stream-10' in groups['molecule']"
        fail_msg: "groups['molecule'] = {{ groups['molecule'] | default([]) }}"

- name: Probe each managed host
  hosts: molecule
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
```

- [ ] **Step 6: `extensions/molecule/podman/verify.yml`**

```yaml
---
- name: Verify each host has sudo installed (prepare side-effect)
  hosts: molecule
  gather_facts: false
  tasks:
    - name: Check sudo
      ansible.builtin.command: which sudo
      changed_when: false
```

- [ ] **Step 7: Smoke-run molecule directly (independent of pytest)**

```bash
cd extensions/molecule/podman
ANSIBLE_COLLECTIONS_PATH="$HOME${ANSIBLE_COLLECTIONS_PATH:+:$ANSIBLE_COLLECTIONS_PATH}" \
PROVISIONER=podman \
molecule test
cd -
```

Expected: full lifecycle passes. Runtime ~2-5 minutes depending on image pulls.

If create fails: look for image-pull errors (you may need to pre-pull on a slow connection). If converge fails: re-read the assert messages — most likely the molecule group wasn't refreshed.

- [ ] **Step 8: Run via the existing pytest harness**

```bash
cd /workspace/ansible-collection-molecule_provisioners
pytest tests/integration -v -k podman
```

Expected: one test passes (the podman scenario). The pytest_ansible `molecule_scenario` fixture discovers `extensions/molecule/podman/` automatically.

- [ ] **Step 9: Commit**

```bash
git add extensions/molecule/podman/
git commit -m "$(cat <<'EOF'
Add podman self-test scenario

Two platforms (Ubuntu 24, CentOS Stream 10), runs the full molecule
lifecycle against the collection's own playbooks via FQCN
import_playbook. Picked up automatically by pytest_ansible's
molecule_scenario fixture.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Self-test scenario — `extensions/molecule/kubevirt/` (gated)

**Files:**

- Create: `extensions/molecule/kubevirt/molecule.yml`
- Create: `extensions/molecule/kubevirt/create.yml`
- Create: `extensions/molecule/kubevirt/destroy.yml`
- Create: `extensions/molecule/kubevirt/prepare.yml`
- Create: `extensions/molecule/kubevirt/converge.yml`
- Create: `extensions/molecule/kubevirt/verify.yml`
- Create: `extensions/molecule/kubevirt/group_vars/all.yml`

- [ ] **Step 1: `extensions/molecule/kubevirt/molecule.yml`**

```yaml
---
driver:
  name: default
  options:
    managed: true
    ansible_connection_options:
      connection: ssh

platforms:
  - name: ubuntu-24
    kubevirt:
      image: quay.io/containerdisks/ubuntu:24.04
      namespace: "${MOLECULE_NAMESPACE:-molecule}"
      ssh_service:
        type: NodePort
      ansible_user: cloud-user
      memory: 4Gi
      disk_size: 30Gi
  - name: centos-stream-10
    kubevirt:
      image: quay.io/containerdisks/centos-stream:10
      namespace: "${MOLECULE_NAMESPACE:-molecule}"
      ssh_service:
        type: NodePort
      ansible_user: cloud-user
      memory: 4Gi
      disk_size: 30Gi

provisioner:
  name: ansible
  playbooks:
    create: create.yml
    destroy: destroy.yml
    prepare: prepare.yml
    converge: converge.yml
    verify: verify.yml
  env:
    PROVISIONER: kubevirt
  inventory:
    links:
      group_vars: group_vars/

verifier:
  name: ansible

scenario:
  name: kubevirt
  test_sequence:
    - dependency
    - syntax
    - create
    - prepare
    - converge
    - verify
    - destroy
```

- [ ] **Step 2: `extensions/molecule/kubevirt/group_vars/all.yml`**

```yaml
---
ansible_connection: ssh
```

- [ ] **Step 3: `create.yml`, `destroy.yml`, `prepare.yml`**

Each is one line — same as podman:

```yaml
---
- import_playbook: david_igou.molecule_provisioners.create
```

(repeat for `destroy.yml` and `prepare.yml` with the matching FQCN)

- [ ] **Step 4: `converge.yml`**

Same as podman's converge.yml — asserts molecule group composition and pings each host. Reproduce inline:

```yaml
---
- name: Verify molecule group composition
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Assert molecule group has both expected hosts
      ansible.builtin.assert:
        that:
          - "'molecule' in groups"
          - "'ubuntu-24' in groups['molecule']"
          - "'centos-stream-10' in groups['molecule']"
        fail_msg: "groups['molecule'] = {{ groups['molecule'] | default([]) }}"

- name: Probe each managed host
  hosts: molecule
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
```

- [ ] **Step 5: `verify.yml`**

KubeVirt prepare doesn't install sudo (it just `wait_for_connection`s). For kubevirt the verify is just basic reachability:

```yaml
---
- name: Verify each host is reachable
  hosts: molecule
  gather_facts: false
  tasks:
    - name: Ping
      ansible.builtin.ping:
```

- [ ] **Step 6: Commit**

```bash
git add extensions/molecule/kubevirt/
git commit -m "$(cat <<'EOF'
Add kubevirt self-test scenario (gated by MOLECULE_KUBEVIRT_ENABLED)

Mirrors the podman scenario but for KubeVirt. Two platforms (Ubuntu
24, CentOS Stream 10) with NodePort SSH services. Skipped by default
in pytest unless MOLECULE_KUBEVIRT_ENABLED is set (gating logic
added in the next commit).

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: Conftest gate for kubevirt self-test

**Files:**

- Create: `tests/integration/conftest.py`

- [ ] **Step 1: Write the conftest**

```python
"""Integration test gating.

Skips the kubevirt molecule scenario unless MOLECULE_KUBEVIRT_ENABLED
is truthy. The pytest_ansible molecule_scenario fixture parameterizes
each scenario directory under extensions/molecule/, so we add a skip
marker to any test item whose nodeid mentions kubevirt.
"""

from __future__ import annotations

import os

import pytest


_TRUTHY = {"1", "true", "yes", "on"}


def _kubevirt_enabled() -> bool:
    return os.environ.get("MOLECULE_KUBEVIRT_ENABLED", "").lower() in _TRUTHY


def pytest_collection_modifyitems(config, items):  # noqa: ARG001
    if _kubevirt_enabled():
        return
    skip = pytest.mark.skip(
        reason="kubevirt scenario gated by MOLECULE_KUBEVIRT_ENABLED",
    )
    for item in items:
        if "kubevirt" in item.nodeid:
            item.add_marker(skip)
```

- [ ] **Step 2: Verify with the gate off**

```bash
cd /workspace/ansible-collection-molecule_provisioners
pytest tests/integration -v --collect-only
```

Expected: both `podman` and `kubevirt` tests are listed; `kubevirt` is marked SKIPPED in `--collect-only` headers.

- [ ] **Step 3: Verify with the gate on (collect-only, no actual cluster)**

```bash
MOLECULE_KUBEVIRT_ENABLED=1 pytest tests/integration -v --collect-only
```

Expected: both tests collected, neither pre-skipped. (Actually running the kubevirt test still requires a cluster.)

- [ ] **Step 4: Commit**

```bash
git add tests/integration/conftest.py
git commit -m "$(cat <<'EOF'
Gate kubevirt molecule scenario behind MOLECULE_KUBEVIRT_ENABLED

CI runners don't have a KubeVirt cluster, so by default the kubevirt
self-test is skipped. Set MOLECULE_KUBEVIRT_ENABLED=1 (or true/yes/on)
locally to exercise it.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: `docs/examples/` — copy-paste starter

**Files:**

- Create: `docs/examples/create.yml`
- Create: `docs/examples/destroy.yml`
- Create: `docs/examples/prepare.yml`
- Create: `docs/examples/molecule.yml`
- Create: `docs/examples/platforms.yml`

- [ ] **Step 1: One-liner playbooks**

```yaml
# docs/examples/create.yml
---
- import_playbook: david_igou.molecule_provisioners.create
```

```yaml
# docs/examples/destroy.yml
---
- import_playbook: david_igou.molecule_provisioners.destroy
```

```yaml
# docs/examples/prepare.yml
---
- import_playbook: david_igou.molecule_provisioners.prepare
```

- [ ] **Step 2: `docs/examples/molecule.yml`**

```yaml
---
# Starter molecule.yml — copy into your collection's
# extensions/molecule/<scenario>/ directory and adjust platforms.

dependency:
  name: galaxy
  options:
    requirements-file: requirements.yml
    force: true

driver:
  name: default
  options:
    managed: true
    ansible_connection_options:
      connection: local

platforms:
  - name: ubuntu-24
    podman:
      image: docker.io/geerlingguy/docker-ubuntu2404-ansible:latest
      command: sleep 1d
    kubevirt:
      image: quay.io/containerdisks/ubuntu:24.04
      namespace: "${MOLECULE_NAMESPACE:-molecule}"
      ssh_service:
        type: NodePort
      ansible_user: cloud-user
      memory: 4Gi
      disk_size: 30Gi

provisioner:
  name: ansible
  playbooks:
    create: create.yml
    destroy: destroy.yml
    prepare: prepare.yml
    converge: converge.yml
    verify: verify.yml
  # Switch backend at runtime: PROVISIONER=podman|kubevirt molecule test
  env: {}

verifier:
  name: ansible

scenario:
  name: default
  test_sequence:
    - dependency
    - syntax
    - create
    - prepare
    - converge
    - verify
    - destroy
```

- [ ] **Step 3: `docs/examples/platforms.yml`**

```yaml
---
# Reference platform schema for david_igou.molecule_provisioners.
# Each platform has both `podman:` and `kubevirt:` blocks so the same
# molecule.yml works under either PROVISIONER.

platforms:
  - name: ubuntu-24 # required (used as instance/host name)
    podman: # required when PROVISIONER=podman
      image: docker.io/...:tag # required
      command: sleep 1d # optional (default: image's CMD)
      privileged: false # optional (default: false)
      volumes: [] # optional, list of "src:dst[:opts]"
      capabilities: [] # optional, e.g. [SYS_ADMIN]
      podman_network: [] # optional, list or single string
      env: {} # optional
      tmpfs: [] # optional
      exposed_ports: [] # optional
      published_ports: [] # optional, "host:container[/proto]"

    kubevirt: # required when PROVISIONER=kubevirt
      image: quay.io/containerdisks/ubuntu:24.04 # required (containerdisk)
      namespace: molecule # required
      ansible_user: cloud-user # required (cloud-init creates this user)
      memory: 4Gi # required
      disk_size: 30Gi # required
      ssh_service:
        type: NodePort # only NodePort supported in v1.0
```

- [ ] **Step 4: Commit**

```bash
git add docs/examples/
git commit -m "$(cat <<'EOF'
Add docs/examples/ starter scenario

Five files consumers can copy into their extensions/molecule/<scenario>/
directory: the three one-liner lifecycle playbooks, a starter
molecule.yml, and a reference platforms.yml documenting every
schema key inline.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 12: `docs/MIGRATION.md`

**Files:**

- Create: `docs/MIGRATION.md`

- [ ] **Step 1: Write the migration doc**

```markdown
# Migrating from a per-repo `extensions/molecule/provisioners/` tree

If your collection currently has `extensions/molecule/provisioners/{podman,kubevirt}/{create,destroy,prepare,requirements}.yml` (the pattern from `david-igou/ansible-collection-devhost`), here's how to switch to using `david_igou.molecule_provisioners`.

## Before
```

your-collection/
└── extensions/molecule/
├── provisioners/
│ ├── podman/
│ │ ├── create.yml
│ │ ├── destroy.yml
│ │ ├── prepare.yml
│ │ ├── requirements.yml
│ │ └── group_vars/...
│ └── kubevirt/
│ ├── create.yml
│ ├── destroy.yml
│ ├── prepare.yml
│ ├── requirements.yml
│ └── group_vars/...
└── default/
├── molecule.yml (points at ../provisioners/${PROVISIONER:-podman}/...)
├── converge.yml
└── verify.yml

```

## After

```

your-collection/
├── requirements.yml (now lists david_igou.molecule_provisioners)
└── extensions/molecule/default/
├── create.yml (one-liner)
├── destroy.yml (one-liner)
├── prepare.yml (one-liner)
├── molecule.yml (points at create.yml/destroy.yml/prepare.yml)
├── converge.yml
└── verify.yml

````

## Steps

### 1. Add the dependency

In your `requirements.yml`:

```yaml
collections:
  - name: david_igou.molecule_provisioners
    version: ">=1.0.0,<2.0.0"
````

Transitive deps (`containers.podman`, `kubernetes.core`, `community.crypto`) are pulled in automatically by the collection's `galaxy.yml` — you can remove the per-provisioner `requirements.yml` files.

### 2. Replace each scenario's lifecycle files

For every directory under `extensions/molecule/<scenario>/`:

```yaml
# create.yml
---
- import_playbook: david_igou.molecule_provisioners.create
```

```yaml
# destroy.yml
---
- import_playbook: david_igou.molecule_provisioners.destroy
```

```yaml
# prepare.yml
---
- import_playbook: david_igou.molecule_provisioners.prepare
```

### 3. Update each scenario's `molecule.yml`

- Repoint `dependency.options.requirements-file` at your top-level `requirements.yml` (or remove the `dependency` block and rely on a CI step `ansible-galaxy collection install -r requirements.yml`).
- Change the playbook references:
  ```yaml
  provisioner:
    name: ansible
    playbooks:
      create: create.yml # was: ../provisioners/${PROVISIONER:-podman}/create.yml
      destroy: destroy.yml # was: ../provisioners/${PROVISIONER:-podman}/destroy.yml
      prepare: prepare.yml # was: ../provisioners/${PROVISIONER:-podman}/prepare.yml
      converge: converge.yml
      verify: verify.yml
  ```
- Remove `inventory.links.group_vars` if it pointed at `../provisioners/<name>/group_vars/`. The collection's roles handle their own internal vars.

### 4. Delete the old tree

```bash
rm -rf extensions/molecule/provisioners/
```

### 5. Verify

```bash
ansible-galaxy collection install -r requirements.yml
PROVISIONER=podman   molecule test -s default
# If you have a KubeVirt cluster:
PROVISIONER=kubevirt molecule test -s default
```

Both should pass without further changes. If they don't, your platform schema may be missing required keys — check `docs/examples/platforms.yml` in this collection for the canonical reference.

## Platform schema differences from the legacy provisioners tree

The collection's platform schema is **identical** to what devhost uses today (multi-keyed `podman:` + `kubevirt:` blocks under each platform). No platform-list edits are required to migrate.

````

- [ ] **Step 2: Commit**

```bash
git add docs/MIGRATION.md
git commit -m "$(cat <<'EOF'
Add docs/MIGRATION.md

Step-by-step for converting a devhost-style collection (with
extensions/molecule/provisioners/) into a consumer of this
collection. Five steps, copy-pasteable commands.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
````

---

## Task 13: CI integration job

**Files:**

- Modify: `.github/workflows/tests.yml`

- [ ] **Step 1: Read current workflow**

```bash
cat .github/workflows/tests.yml
```

You'll see the existing reusable-workflow jobs (changelog, build-import, ansible-lint, sanity, unit-galaxy, unit-source) plus an `all_green` aggregator.

- [ ] **Step 2: Add an integration job**

Edit `.github/workflows/tests.yml`. Add this `integration` job before `all_green`:

```yaml
  integration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout into the canonical collection path
        uses: actions/checkout@v4
        with:
          path: ansible_collections/david_igou/molecule_provisioners

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install ansible + molecule + pytest plumbing
        working-directory: ansible_collections/david_igou/molecule_provisioners
        run: |
          python -m pip install --upgrade pip
          pip install ansible-core molecule "molecule-plugins[podman]" \
                      pytest pytest-ansible pytest-xdist

      - name: Install collection dependencies
        working-directory: ansible_collections/david_igou/molecule_provisioners
        run: ansible-galaxy collection install -r <(echo "
          collections:
            - name: containers.podman
            - name: kubernetes.core
            - name: community.crypto
          ")

      - name: Run podman integration tests
        working-directory: ansible_collections/david_igou/molecule_provisioners
        env:
          PROVISIONER: podman
          ANSIBLE_COLLECTIONS_PATH: ${{ github.workspace }}
        run: pytest tests/integration -v
```

Then add `integration` to the `all_green.needs` list and the `python -c` assertion:

```yaml
all_green:
  if: ${{ always() }}
  needs:
    - changelog
    - build-import
    - sanity
    - unit-galaxy
    - unit-source
    - ansible-lint
    - integration
  runs-on: ubuntu-latest
  steps:
    - run: >-
        python -c "assert 'failure' not in
        set([
        '${{ needs.changelog.result }}',
        '${{ needs.sanity.result }}',
        '${{ needs.unit-galaxy.result }}',
        '${{ needs.ansible-lint.result }}',
        '${{ needs.unit-source.result }}',
        '${{ needs.integration.result }}'
        ])"
```

(Note: also fix the existing missing commas in the python -c expression while you're there — the original has `'${{ needs.unit-galaxy.result }}'` then `'${{ needs.ansible-lint.result }}'` adjacent without a comma, which makes Python concatenate them silently. The version above adds the missing commas.)

- [ ] **Step 3: yamllint the workflow**

```bash
yamllint .github/workflows/tests.yml
```

Expected: clean.

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/tests.yml
git commit -m "$(cat <<'EOF'
CI: add integration job (podman self-test) and fix all_green commas

The new integration job checks out the repo into the canonical
ansible_collections/david_igou/molecule_provisioners/ layout, installs
ansible + molecule with the podman plugin, installs collection
dependencies, and runs pytest tests/integration with PROVISIONER=podman.
The kubevirt scenario is skipped by the conftest gate.

Also fixes missing commas in the all_green python -c expression that
silently concatenated adjacent result strings.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 14: Update `CLAUDE.md`

**Files:**

- Modify: `CLAUDE.md`

- [ ] **Step 1: Replace `CLAUDE.md` with the new architecture**

Overwrite the file with:

````markdown
# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project type

Ansible Collection `david_igou.molecule_provisioners`. Provides reusable Molecule provisioner playbooks and roles (podman, kubevirt) so other collections can test themselves without copy-pasting `create.yml`/`destroy.yml`/`prepare.yml` per repo. Targets `ansible-core >= 2.15`.

The collection FQCN appears throughout (`david_igou.molecule_provisioners.create`, etc.). Tooling requires it to live at `ansible_collections/david_igou/molecule_provisioners/` somewhere on `ANSIBLE_COLLECTIONS_PATH`. If working outside that layout, symlink the repo into one:

```bash
mkdir -p "$HOME/ansible_collections/david_igou"
ln -snf "$PWD" "$HOME/ansible_collections/david_igou/molecule_provisioners"
export ANSIBLE_COLLECTIONS_PATH="$HOME"
```

## Architecture (one-paragraph version)

Three top-level dispatcher playbooks (`playbooks/{create,destroy,prepare}.yml`) read `$PROVISIONER` (default `podman`), validate platform shape, and `include_role` into one of two roles (`roles/podman`, `roles/kubevirt`). Each role uses `tasks_from` for lifecycle dispatch. Consumers' scenario `create.yml`/`destroy.yml`/`prepare.yml` are one-liners that `import_playbook: david_igou.molecule_provisioners.<phase>`. Public contract: each platform in `molecule.yml` has multi-keyed `podman:` and/or `kubevirt:` blocks; same `molecule.yml` works under either backend by switching `$PROVISIONER`.

### Key files

- `playbooks/{create,destroy,prepare}.yml` — dispatcher entry points; the `import_playbook` targets that consumers reference by FQCN.
- `roles/podman/tasks/{create,destroy,prepare,_networks}.yml` — podman lifecycle. `_networks.yml` is shared between create and destroy.
- `roles/kubevirt/tasks/{create,destroy,prepare,_create_vm,_create_vm_dictionary}.yml` — kubevirt lifecycle. `_create_vm*.yml` are per-platform helpers included in a loop with `loop_var: vm`.
- `extensions/molecule/{podman,kubevirt}/` — self-test scenarios. Discovered by `pytest_ansible.molecule_scenario` fixture in `tests/integration/test_integration.py`.
- `tests/integration/conftest.py` — skips kubevirt unless `MOLECULE_KUBEVIRT_ENABLED` is truthy.
- `docs/examples/` — copy-paste starter for consumers.
- `docs/MIGRATION.md` — converting devhost-style consumers.

## Common commands

| Task                                   | Command                                                               |
| -------------------------------------- | --------------------------------------------------------------------- |
| Install runtime/test deps              | `pip install -r requirements.txt -r test-requirements.txt`            |
| Lint everything                        | `ansible-lint && yamllint .`                                          |
| Run podman self-test                   | `pytest tests/integration -v -k podman`                               |
| Run a single scenario directly         | `cd extensions/molecule/podman && PROVISIONER=podman molecule test`   |
| Run kubevirt self-test (needs cluster) | `MOLECULE_KUBEVIRT_ENABLED=1 pytest tests/integration -v -k kubevirt` |
| Ansible sanity                         | `ansible-test sanity --docker` (run from the symlink path)            |
| Build collection artifact              | `ansible-galaxy collection build`                                     |
| Pre-commit                             | `pre-commit run --all-files`                                          |

`pyproject.toml` configures pytest with `-n 2` (xdist parallel). `tests/integration/conftest.py` filters scenarios by env var.

## Public contract (the thing we don't break without a major bump)

The platform schema in `molecule.yml`:

```yaml
platforms:
  - name: <str> # required
    podman: # required when PROVISIONER=podman
      image: <str> # required
      # optional: command, privileged, volumes, capabilities,
      # podman_network, env, tmpfs, exposed_ports, published_ports
    kubevirt: # required when PROVISIONER=kubevirt
      image: <str> # required (containerdisk)
      namespace: <str> # required
      ansible_user: <str> # required
      memory: <str> # required
      disk_size: <str> # required
      ssh_service:
        type: NodePort # only NodePort in v1
```

Breaking changes to the above keys → major version bump. New optional keys → minor.

## When updating provisioner logic

1. Make changes in the role (`roles/<backend>/tasks/`).
2. Run `ansible-lint roles/<backend>/`.
3. Run the self-test scenario: `cd extensions/molecule/<backend> && molecule test`.
4. If the change affects the platform schema, also update:
   - `roles/<backend>/meta/argument_specs.yml`
   - `roles/<backend>/README.md`
   - `docs/examples/platforms.yml`
   - the schema section above

## Pre-commit

Runs `update-docs` (collection_prep), `prettier`, `isort`, `black`, `flake8`, plus `no-commit-to-branch` against `main`. Don't bypass with `--no-verify`.

## CI

`.github/workflows/tests.yml` runs the reusable workflows from `ansible/ansible-content-actions` (changelog, build-import, ansible-lint, sanity, unit-galaxy) plus `unit-source` and an `integration` job that exercises the podman scenario via pytest. `release.yml` publishes to Galaxy on GitHub release.

## Out of scope (per the v1.0 spec)

docker / qemu / libvirt / cloud backends, LoadBalancer kubevirt service types, Windows guests, Molecule `shared_state` pattern. See `docs/superpowers/specs/2026-05-08-molecule-provisioners-design.md` for the design discussion.
````

- [ ] **Step 2: Commit**

```bash
git add CLAUDE.md
git commit -m "$(cat <<'EOF'
Rewrite CLAUDE.md for the v1.0 architecture

Replaces the scaffold-oriented description with: how the dispatcher
+ role-tasks_from pattern works, the public platform schema, the
self-test scenarios, the kubevirt env gate, and what to update when
modifying provisioner logic.

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 15: Add changelog fragment for v1.0.0

**Files:**

- Create: `changelogs/fragments/v1.0.0.yml`

- [ ] **Step 1: Write the fragment**

```yaml
---
release_summary: |
  Initial release. Reusable Molecule provisioner playbooks and roles
  for podman and kubevirt backends. Consumers' scenario lifecycle
  files become one-line ``import_playbook`` wrappers; backend selected
  via ``$PROVISIONER`` (default ``podman``).
major_changes:
  - Initial v1.0.0 release.
```

- [ ] **Step 2: Regenerate `CHANGELOG.rst`**

```bash
antsibull-changelog release --version 1.0.0 || true
```

If `antsibull-changelog` isn't installed, `pip install antsibull-changelog` first. The command writes `CHANGELOG.rst` and `changelogs/changelog.yaml`.

If `||  true` swallowed an error: investigate. Common cause: missing fields in `galaxy.yml` — run `antsibull-changelog --help` and check the output of `antsibull-changelog lint` for hints.

- [ ] **Step 3: Verify changelog**

```bash
head -30 CHANGELOG.rst
```

Expected: a heading "v1.0.0" with the release summary text.

- [ ] **Step 4: Commit**

```bash
git add changelogs/fragments/v1.0.0.yml CHANGELOG.rst changelogs/changelog.yaml
git commit -m "$(cat <<'EOF'
Add v1.0.0 changelog fragment and regenerate CHANGELOG.rst

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 16: End-to-end verification and pre-release manual gate

**No file changes** — verification only.

- [ ] **Step 1: Full local test suite**

```bash
cd /workspace/ansible-collection-molecule_provisioners
ansible-lint
yamllint .
pytest tests/unit -v
pytest tests/integration -v   # podman only by default
```

Expected: all green.

- [ ] **Step 2: Build the collection artifact**

```bash
ansible-galaxy collection build --output-path /tmp
ls -la /tmp/david_igou-molecule_provisioners-1.0.0.tar.gz
tar tzf /tmp/david_igou-molecule_provisioners-1.0.0.tar.gz | head -40
```

Expected: archive exists; contents include `playbooks/`, `roles/podman/`, `roles/kubevirt/`, `meta/`, `MANIFEST.json`. Verify `docs/superpowers/` is NOT present (build_ignore).

- [ ] **Step 3: Pre-release manual gate (devhost migration sanity)**

This is the spec's pre-release gate. Outside this repo, take a copy of `david-igou/ansible-collection-devhost`:

```bash
cd /tmp
gh repo clone david-igou/ansible-collection-devhost
cd ansible-collection-devhost
```

Replace `extensions/molecule/default/{create,destroy,prepare}.yml` with one-line versions (or remove the references and add new ones — follow `docs/MIGRATION.md` from this collection). Update `extensions/molecule/default/molecule.yml` accordingly. Add `david_igou.molecule_provisioners` to `requirements.yml`.

Then:

```bash
ansible-galaxy collection install -r requirements.yml
ansible-galaxy collection install /tmp/david_igou-molecule_provisioners-1.0.0.tar.gz --force
PROVISIONER=podman molecule test -s default
```

Expected: passes without further changes.

If it fails: capture the error, return to this collection, fix, increment version, rerun.

- [ ] **Step 4: Tag and push**

(Only after Step 3 passes.)

```bash
cd /workspace/ansible-collection-molecule_provisioners
git tag -a v1.0.0 -m "v1.0.0 — initial release"
git log --oneline | head -5
```

Pushing and creating the GitHub release is **outside** this plan — confirm with the user before doing either. The existing `release.yml` workflow publishes to Galaxy on `release: published`, so the user creates the release on GitHub manually after reviewing the tag.

---

## End of plan

Sixteen tasks. After Task 14 the collection is feature-complete; Tasks 15-16 are release prep and verification. No task modifies more than ~6 files; each commit is a coherent unit.
