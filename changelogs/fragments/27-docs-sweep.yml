---
trivial:
  - >-
    docs: address feedback from #27 — dropped the stale "no docker" line
    from ``docs/MIGRATION.md``'s NOT-supported list; lead the README
    install instructions with the git-URL ``collections.yml`` form that
    works today (the bare ``ansible-galaxy collection install`` line
    404s until the first Galaxy release); name the per-scenario file
    ``collections.yml`` explicitly (molecule 25.x looks for that, not
    ``requirements.yml``); add an RBAC table to the kubevirt role README
    with the minimal verb set the role needs; note that ``oc auth can-i``
    is authoritative on OpenShift (#28, #29, #32, #34, #38).
